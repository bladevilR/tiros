<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.basemanage.tpplan.mapper.BuTpRepairPlanPersonMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="org.jeecg.modules.basemanage.tpplan.bean.BuTpRepairPlanPerson">
        <result column="id" property="id"/>
        <result column="task_id" property="taskId"/>
        <result column="amount" property="amount"/>
        <result column="require_postion" property="requirePostion"/>
        <result column="require_certificate" property="requireCertificate"/>
        <result column="require_tech" property="requireTech"/>
        <result column="remark" property="remark"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, task_id, amount, require_postion, require_certificate, require_tech, remark
    </sql>

    <!-- 插入value sql -->
    <sql id="insertValueItems">
        <trim suffixOverrides=",">
            #{item.id},
            #{item.taskId},
            #{item.amount},
            #{item.requirePostion},
            #{item.requireCertificate},
            #{item.requireTech},
            #{item.remark},
        </trim>
    </sql>


    <insert id="insertList" parameterType="java.util.List">
        <if test="list != null and list.size > 0">
            INSERT INTO bu_tp_repair_plan_person
            (
            <include refid="Base_Column_List"/>
            )
            VALUES
            <foreach collection="list" item="item" index="index" separator=",">
                (
                <include refid="insertValueItems"/>
                )
            </foreach>
        </if>
    </insert>

    <select id="selectListByPlanId" resultType="org.jeecg.modules.basemanage.tpplan.bean.BuTpRepairPlanPerson">
        select t.*,
               t1.task_name,
               t1.work_group_id,
               t2.group_name as workGroupName
        from bu_tp_repair_plan_person t
                 left join bu_tp_repair_plan_task t1 on t1.id = t.task_id
                 left join bu_mtr_workshop_group t2 on t2.id = t1.work_group_id
        where t1.plan_id = #{planId}
        order by t1.work_group_id, t.require_postion
    </select>

    <select id="selectListByTaskId" resultType="org.jeecg.modules.basemanage.tpplan.bean.BuTpRepairPlanPerson">
        select t.*,
               t1.task_name,
               t1.work_group_id,
               t2.group_name as workGroupName
        from bu_tp_repair_plan_person t
                 left join bu_tp_repair_plan_task t1 on t1.id = t.task_id
                 left join bu_mtr_workshop_group t2 on t2.id = t1.work_group_id
        where t.task_id = #{taskId}
        order by t.require_postion
    </select>

</mapper>
