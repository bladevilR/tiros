<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.material.returned.mapper.BuMaterialReturnedDetailMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="org.jeecg.modules.material.returned.bean.BuMaterialReturnedDetail">
        <id column="id" property="id"/>
        <result column="returned_id" property="returnedId"/>
        <result column="work_order_id" property="workOrderId"/>
        <result column="material_type_id" property="materialTypeId"/>
        <result column="order_material_id" property="orderMaterialId"/>
        <result column="return_amount" property="returnAmount"/>
        <result column="location_wbs" property="locationWbs"/>
        <result column="location_name" property="locationName"/>
        <result column="ebs_wh_code" property="ebsWhCode"/>
        <result column="ebs_wh_child_code" property="ebsWhChildCode"/>
        <result column="trade_no" property="tradeNo"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, returned_id, work_order_id, material_type_id, order_material_id, return_amount,
        location_wbs, location_name, ebs_wh_code, ebs_wh_child_code, trade_no
    </sql>

    <!-- 插入value sql -->
    <sql id="insertValueItems">
        <trim suffixOverrides=",">
            #{item.id, jdbcType=VARCHAR},
            #{item.returnedId, jdbcType=VARCHAR},
            #{item.workOrderId, jdbcType=VARCHAR},
            #{item.materialTypeId, jdbcType=VARCHAR},
            #{item.orderMaterialId, jdbcType=VARCHAR},
            #{item.returnAmount, jdbcType=DOUBLE},
            #{item.locationWbs, jdbcType=VARCHAR},
            #{item.locationName, jdbcType=VARCHAR},
            #{item.ebsWhCode, jdbcType=VARCHAR},
            #{item.ebsWhChildCode, jdbcType=VARCHAR},
            #{item.tradeNo, jdbcType=VARCHAR},
        </trim>
    </sql>


    <insert id="insertList" parameterType="java.util.List">
        <if test="list != null and list.size > 0">
            INSERT INTO bu_material_returned_detail
            (
            <include refid="Base_Column_List"/>
            )
            <foreach collection="list" item="item" index="index" separator="union all">
                (
                select
                <include refid="insertValueItems"/>
                from dual
                )
            </foreach>
        </if>
    </insert>

    <select id="selectListByReturnedId" resultType="org.jeecg.modules.material.returned.bean.BuMaterialReturnedDetail">
        select t.*,
               t1.code    as materialTypeCode,
               t1.name    as materialTypeName,
               t1.spec    as materialTypeSpec,
               t1.unit    as materialTypeUnit,
               t2.receive as applyAmount,
               t3.plan_id,
               t3.line_id,
               t3.train_no,
               t3.group_id,
               t3.order_code,
               t3.order_type
        from bu_material_returned_detail t
                 left join bu_material_type t1 on t1.id = t.material_type_id
                 left join bu_material_apply_detail t2 on t2.id = t.order_material_id
                 left join bu_work_order t3 on t3.id = t.work_order_id
        where t.returned_id = #{returnedId}
    </select>

    <select id="selectSimpleListByReturnedIdList"
            resultType="org.jeecg.modules.material.returned.bean.BuMaterialReturnedDetail">
        SELECT
        t.*,
        NVL(t1.unit_price, 0) as materialUnitPrice,
        t2.group_id,
        t3.code as materialTypeCode,
        t3.name as materialTypeName,
        t4.plan_id,
        t4.line_id,
        t4.train_no,
        t4.group_id,
        t4.order_code,
        t4.order_type
        FROM bu_material_returned_detail t
        left join bu_material_apply_detail t1 on t1.id = t.order_material_id
        left join bu_material_returned t2 on t2.id = t.returned_id
        left join bu_material_type t3 on t3.id = t.material_type_id
        left join bu_work_order t4 on t4.id = t.work_order_id
        where t.returned_id
        <choose>
            <when test="returnedIdList != null and returnedIdList.size >0">
                in
                <foreach collection="returnedIdList" item="returnedId" index="index" separator="," open="("
                         close=")">
                    #{returnedId}
                </foreach>
            </when>
            <otherwise>
                = '-1'
            </otherwise>
        </choose>
    </select>

    <select id="selectListForMaximoByReturnedIdList"
            resultType="org.jeecg.modules.material.returned.bean.BuMaterialReturnedDetail">
        SELECT t.*,
        t8.code as locationCode,
        t8.sys_house_category as sysHouseCategory,
        t1.use_category,
        t3.train_no,
        t3.order_code,
        t5.code as materialTypeCode,
        t5.name as materialTypeName,
        t5.spec as materialTypeSpec,
        NVL(t1.unit_price, 0) as materialUnitPrice,
        t1.confirm_time,
        t3.line_id,
        t7.depot_id,
        t4.id AS workshopId,
        t4.name as workshopName,
        t6.id AS trainid,
        t7.repair_program_id AS programId,
        t3.fd_project,
        t9.code as fdProjectCode,
        t9.name as fdProjectName,
        t3.fd_task,
        t10.code as fdTaskCode,
        t10.name as fdTaskName,
        t3.fd_cost_type
        FROM bu_material_returned_detail t
        left join bu_material_apply_detail t1 on t1.id = t.order_material_id
        left join bu_material_returned t2 on t2.id = t.returned_id
        LEFT JOIN bu_work_order t3 ON t3.id = t2.work_order_id
        LEFT JOIN bu_mtr_workshop t4 ON t4.id = t3.workshop_id
        LEFT JOIN bu_material_type t5 ON t5.id = t.material_type_id
        LEFT JOIN bu_train_info t6 on t6.train_no = t3.train_no
        LEFT JOIN bu_repair_plan t7 on t7.id = t3.plan_id
        LEFT JOIN bu_mtr_warehouse t8 on t8.wbs = t.location_wbs
        LEFT JOIN bu_maximo_finance_item t9 on t9.id = t3.fd_project
        LEFT JOIN bu_maximo_finance_item t10 on t10.id = t3.fd_task
        where t.returned_id
        <choose>
            <when test="returnedIdList != null and returnedIdList.size >0">
                in
                <foreach collection="returnedIdList" item="returnedId" index="index" separator="," open="("
                         close=")">
                    #{returnedId}
                </foreach>
            </when>
            <otherwise>
                = '-1'
            </otherwise>
        </choose>
    </select>

</mapper>
