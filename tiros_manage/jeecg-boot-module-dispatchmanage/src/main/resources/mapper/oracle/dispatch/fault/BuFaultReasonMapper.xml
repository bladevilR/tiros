<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.dispatch.fault.mapper.BuFaultReasonMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="org.jeecg.modules.dispatch.fault.bean.BuFaultReason">
        <id column="id" property="id"/>
        <result column="reason_code" property="reasonCode"/>
        <result column="reason_desc" property="reasonDesc"/>
        <result column="category_id" property="categoryId"/>
        <result column="fault_code_id" property="faultCodeId"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, reason_code, reason_desc, category_id, fault_code_id
    </sql>

    <select id="selectPageByCondition" resultType="org.jeecg.modules.dispatch.fault.bean.BuFaultReason">
        select t.* from bu_fault_reason t
        <where>
            <if test="queryVO.searchText != null and queryVO.searchText != ''">
                and
                (
                t.reason_code like  '%'||#{queryVO.searchText}||'%'
                or
                t.reason_desc like '%'||#{queryVO.searchText}||'%'
                )
            </if>
            <if test="queryVO.categoryId != null and queryVO.categoryId != ''">
                and t.category_id = #{queryVO.categoryId}
            </if>
            <if test="queryVO.faultCodeId != null and queryVO.faultCodeId != ''">
                and t.fault_code_id = #{queryVO.faultCodeId}
            </if>
        </where>
    </select>

</mapper>
