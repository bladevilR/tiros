<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.dispatch.display.mapper.BuDisplayScreenMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="org.jeecg.modules.dispatch.display.bean.BuDisplayScreen">
        <id column="id" property="id"/>
        <result column="screen_type" property="screenType"/>
        <result column="screen_code" property="screenCode"/>
        <result column="screen_name" property="screenName"/>
        <result column="address" property="address"/>
        <result column="group_id" property="groupId"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="update_by" property="updateBy"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, screen_type, screen_code, screen_name, address, group_id, remark, create_time, create_by, update_time, update_by
    </sql>

    <select id="selectListByTypeAndGroupId" resultType="org.jeecg.modules.dispatch.display.bean.BuDisplayScreen">
        select
        t.*,
        t1.depart_name as groupName
        from bu_display_screen t
        left join sys_depart t1 on t1.id = t.group_id
        where t.screen_type = #{screenType}
        <if test="screenType != null and screenType == 2">
            <if test="groupId != null and groupId != ''">
                and t.group_id = #{groupId}
            </if>
        </if>
    </select>

</mapper>
