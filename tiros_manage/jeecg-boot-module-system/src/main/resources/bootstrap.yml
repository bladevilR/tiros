spring:
  application:
    name: tiros_manage
  profiles:
    active: prod
  cloud:
    nacos:
      config:
        server-addr: ${nacos.server-addr:127.0.0.1:8848}
        #server-addr: ${nacos.server-addr:106.53.225.45:8848}
        file-extension: yml
        namespace: nannar_tiros
        enabled: false