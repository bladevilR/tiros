<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.system.mapper.SysRoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="org.jeecg.modules.system.entity.SysRole">
        <id column="id" property="id"/>
        <result column="role_name" property="roleName"/>
        <result column="role_code" property="roleCode"/>
        <result column="description" property="description"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, role_name, role_code, description, create_by, create_time, update_by, update_time
    </sql>

    <delete id="deleteRoleUserRelation">
        delete
        from sys_user_role
        where role_id = #{roleId}
    </delete>

    <delete id="deleteRolePermissionRelation">
        delete
        from sys_role_permission
        where role_id = #{roleId}
    </delete>

    <select id="selectByRoleName" resultType="org.jeecg.modules.system.entity.SysRole">
        select *
        from sys_role
        where role_name = #{roleName}
    </select>

</mapper>
